# ========= Static caching =========
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css                 "access plus 1 year"
  ExpiresByType application/javascript   "access plus 1 year"
  ExpiresByType application/x-javascript "access plus 1 year"
  ExpiresByType image/webp               "access plus 1 year"
  ExpiresByType image/svg+xml            "access plus 1 year"
  Options -MultiViews
</IfModule>

<IfModule mod_headers.c>
  # Long cache for static assets (immutable URLs with ?v=)
  <FilesMatch "\.(css|js|webp|svg)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  # No cache for HTML
  <FilesMatch "\.html?$">
    Header set Cache-Control "no-cache, must-revalidate, max-age=0"
  </FilesMatch>
</IfModule>

# ========= Canonical + HTTPS in a single hop =========
RewriteEngine On
# If HTTP OR host is www → redirect to https://oniito.com
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.oniito\.com$ [NC]
RewriteRule ^ https://oniito.com%{REQUEST_URI} [L,R=301]

# (Optional) HSTS — включай только когда уверен, что HTTPS стабилен
# <IfModule mod_headers.c>
#   Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
# </IfModule>
